name: 'Lambda update alias action'

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-run:

    runs-on: ubuntu-latest

    steps:

    - name: Lambda update alias action
      id: lambda-update-alias
      uses: LipatovAlexander/LambdaUpdateAliasAction@main
